import { PlatformAccessory, Logger } from 'homebridge';
import { DreoPlatform } from './platform';
/**
 * Platform Accessory
 * An instance of this class is created for each accessory your platform registers
 * Each accessory may expose multiple services of different service types.
 */
export declare class FanAccessory {
    private readonly platform;
    private readonly accessory;
    private readonly state;
    private readonly ws;
    readonly log: Logger;
    private service;
    private fanState;
    constructor(platform: DreoPlatform, accessory: PlatformAccessory, state: any, ws: any, log: Logger);
    handleActiveSet(value: any): void;
    handleActiveGet(): boolean;
    setRotationSpeed(value: any): Promise<void>;
    getRotationSpeed(): Promise<number>;
    setSwingMode(value: any): Promise<void>;
    getSwingMode(): Promise<boolean>;
}
//# sourceMappingURL=FanAccessory.d.ts.map